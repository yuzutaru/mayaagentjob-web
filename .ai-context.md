# Global System Map: MayaAgentJob

## Core Architecture & Multi-Repo Ecosystem
You are working within a hybrid multi-repo workspace unified via a local Filesystem MCP Server. All repositories strictly adhere to Feature-Based Modular Clean Architecture principles and stable 2026 industry best practices.
- `mayaagentjob-backend/`: [Private] Supabase core database, edge functions, and AI scoring logic built with modular serverless clean architecture.
- `mayaagentjob-mobile/`: [Public] Unified cross-platform mobile client built with React Native (Expo SDK 50+), TypeScript, and strict MVVM/Hook + Feature-Based Modular Clean Architecture.
- `mayaagentjob-web/`: [Public] Admin/Web portal built with React, Vite, TypeScript, and Feature-Based Modular Clean Architecture.

---

# Local Platform Blueprint: React Web Portal (Maya)

This repository holds the desktop and web portal interface for **Maya**, built as a responsive React single-page dashboard application [cite: 14, 20].

## 🛠️ Technical Stack & Architecture
* **Framework:** React 18 + Vite (Single Page Application configuration with stable modern bundling) [cite: 14, 20].
* **Language:** TypeScript (Strict Mode with ESLint rules enabled for 2026 best practices) [cite: 20].
* **Architecture:** Feature-Based Modular Clean Architecture (Strict vertical slicing into feature modules with modular `presentation`, `domain`, and `data` layers).
* **Styling:** Tailwind CSS (Utility-first, responsive, dark-mode ready).
* **Database & Auth:** Supabase JS Client wrapped in repository layers [cite: 20].

## 🎨 Design System: Web Desktop Blue & White
The desktop layout maximizes screen real estate by using a **Split-Pane Master-Detail Grid** [cite: 36, 45]:
* **Left Sidebar:** Persistent navigation panel (Feed, Saved/Applied, Subscription Tier Status) [cite: 20, 36].
* **Middle Feed:** Dynamic, scrollable layout of matching job cards based on active domain filter [cite: 36, 54].
* **Right Panel:** Interactive Matrix Form (allows real-time profile editing and automatic search feed updates) [cite: 36, 48].

### Tailwind Color Tokens:
* **Light Mode:**
  * Background Canvas: `bg-white` (`#FFFFFF`)
  * Card Wrapper: `bg-slate-50` (`#F8FAFC`) with border `border-slate-200` (`#E2E8F0`)
  * Buttons & Highlights: `bg-blue-700` (`#1D4ED8`) or `hover:bg-blue-800`
  * Text Primary: `text-slate-900` (`#0F172A`)
  * Text Secondary: `text-slate-600` (`#475569`)
* **Dark Mode (`dark:` modifier):**
  * Background Canvas: `dark:bg-slate-950` (`#020617`)
  * Card Wrapper: `dark:bg-slate-900` (`#0F172A`) with border `dark:border-slate-800`
  * Buttons & Highlights: `dark:bg-blue-600` (`#2563EB`) or `dark:hover:bg-blue-700`
  * Text Primary: `dark:text-white`
  * Text Secondary: `dark:text-slate-400` (`#94A3B8`)

## 📡 Shared TypeScript Contracts
Ensure standard DTO definitions are structured for easy API parsing and component properties [cite: 23]:

```typescript
export interface UserProfile {
  targetDomains: string[];
  technicalStack: string[];
  preferences: {
    arrangements: string[];
    targetRegions: string[];
    requiresVisaSponsorship: boolean;
    openToContract: boolean;
  };
}

export interface JobListingCard {
  jobId: string;
  title: string;
  company: string;
  arrangement: string;
  location: string;
  matchScore: number;
  aiSummaryBullets: string[];
}
```

## 🔒 Session & Entitlement Handling
* **Authentication:** Supabase Auth SDK utilizing GitHub Social Login (the primary onboarding conversion action) [cite: 34, 53].
* **Entitlement Model (No Direct Stripe Checkout):** This portal does not manage standalone checkout gateways in Phase 1 [cite: 20]. Instead, it checks the authenticated Supabase user profile table (`profiles.subscription_status`) [cite: 20]. Premium features (such as daily credits and detailed AI breakdowns) are unlocked solely if a valid subscription was initiated via the native iOS/Android mobile clients [cite: 20, 81].

## Local Project Directory Architecture
- `mayaagentjob-web/`
  - `src/`
    - `core/`                 # Core infrastructure (base Supabase client, HTTP API wrappers, theme tokens)
    - `profile/`              # Top-level Profile module (presentation/, domain/, data/)
    - `jobs-matching/`        # Top-level Job Matching module (presentation/, domain/, data/)
    - `auth/`                 # Top-level Auth module (presentation/, domain/, data/)
    - `shared/`               # Shared utilities, common UI components, and design system tokens
  - `vite.config.ts`          # Vite bundling and environment configuration
  - `.ai-context.md`          # Current active platform context blueprint

## Strict Local Rules for Code Generation
1. **No Checkout Forms:** Do not code standalone payment processing gateways, transaction buttons, or checkout forms in this web repository. The web application acts purely as an evaluative/administrative view.
2. **Entitlement Enforcement:** To verify if a user has unlocked premium matching utilities, execute a standard query on the `profiles.subscription_status` data column. Entitlements are populated exclusively through mobile native execution pathways.
3. **Session Management:** Securely handle Supabase Auth token distribution across routes. Prevent layouts from rendering authenticated profile domains if the underlying session status is invalid.
4. **Autonomous Protocol:** Always output a complete Task List and Implementation Plan Artifact inside your reasoning loop before modifying component hierarchies or writing routes. Refer to [MOBILE_ONBOARDING_IMPLEMENTATION_PLAN.md](file:///Users/yuzutaru/Development/mayaagentjob-workspace/MOBILE_ONBOARDING_IMPLEMENTATION_PLAN.md) in the workspace root as a guideline.
5. **Strict Clean Architecture Layer Isolation (2026 Standard):** UI components and presentation hooks must not make raw database queries or direct ad-hoc API calls. All Supabase interactions and network requests must flow through typed repository services in the Data layer, adhering to Domain contracts for maximum stability and maintainability.
6. **Modular Architecture & Feature Isolation:** Structure frontend code into independent top-level feature modules (`src/profile/`, `src/jobs-matching/`), never nested inside an umbrella feature folder. Maintain shared infrastructure (Supabase client, base API wrappers, theme tokens) inside `src/core/` and `src/shared/`. Do not import components or repository implementations directly across feature boundaries without going through shared domain interfaces.